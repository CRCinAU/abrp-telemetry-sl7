<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android Automotive OS / Car API permissions -->
    <!-- CAR_VENDOR_EXTENSION is "dangerous" and grantable via: adb shell pm grant com.abrp.telemetry android.car.permission.CAR_VENDOR_EXTENSION -->
    <uses-permission android:name="android.car.permission.CAR_VENDOR_EXTENSION" />
    <!-- Keep standard AOSP permissions declared in case a future firmware update makes them accessible -->
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CAR_SPEED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ABRPTelemetry">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Headless stub launched by the shell-user resurrector daemon via
             `am start -n com.abrp.telemetry/.WakeActivity` to un-stop the package
             after BYD's ignition-off forceStopPackage(). finish()es in onCreate. -->
        <activity
            android:name=".WakeActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:finishOnTaskLaunch="true" />

        <service
            android:name=".TelemetryService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Two entry points for the service:
             - BOOT_COMPLETED: after a real head-unit reboot, kick the service if
               telemetry was running before. (Ignition off/on is *not* a reboot —
               that path goes through DaemonLauncher → WakeActivity instead.)
             - AlarmManager periodic ticks: fired via a getBroadcast() PendingIntent
               that targets this receiver explicitly. Drives the steady-state send
               cadence while the service is alive. -->
        <receiver
            android:name=".WakeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
